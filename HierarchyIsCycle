WITH H AS (
	SELECT 'C' AS N, 'B' AS P FROM DUMMY UNION ALL
	SELECT 'B', 'A' FROM DUMMY UNION ALL
	SELECT 'A', NULL FROM DUMMY	/*UNION ALL
	SELECT 'A', 'C' FROM DUMMY*/
)
SELECT *
FROM HIERARCHY (
	SOURCE ( 
		SELECT
			H.N AS NODE_ID, 
			H.P AS PARENT_ID 
		FROM H
	)
);
